import React from 'react'
import './index.scss';

const SubmitButton = () => {
  return (
    <button onClick={console.log(true)} class="submit-button"> Submit Results </button>
  )
}

export default SubmitButton